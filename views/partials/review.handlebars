<div>
    {{#each this}}
    <section>
        <div>
        {{user.username}} on {{format_date created_at}}
        </div>
        <div>
            {{review_text}}
        </div>
    </section>
    {{/each}}
</div>